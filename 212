#include <bits/stdc++.h>
using namespace std;

int main() {
    char a, b;
    cin >> a >> b;

    if (a > b) {
        for (char i = a; i >= b; i--) {
            cout << i << " ";
        }
    } else if (a < b) {
        for (char i = a; i <= b; i++) {
            cout << i << " ";
        }
    } else cout<<a;

    cin.get();
    cin.get();
    return 0;
}
