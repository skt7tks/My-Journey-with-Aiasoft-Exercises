#include<bits/stdc++.h>
using namespace  std;

int main(){
    int n,x=0,y=0;
    cin>>n;
    int a[n];
    for(int i=0;i<n;i++)
        cin>>a[i];
    for(int i=0;i<n;i++)
        if(x<a[i]) {
            x = a[i];
            y++;
        }
    cout<<y;
    return 0;
}
