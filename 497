#include <bits/stdc++.h>
using namespace std;
int main() {
int n = 4;
int a[n];
for (int i=0; i<n; i++) {
    cin>>a[i];
}
sort(a,a+n);
int y=a[0];
int x=0;
for (int i=n-1; i>=0; i--) {
    x=x+a[i];
}
cout<<x+1-y<<endl;
cin.get();
cin.get();
return 0;
}
